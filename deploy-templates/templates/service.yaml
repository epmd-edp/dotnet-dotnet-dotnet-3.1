apiVersion: v1
kind: Service
metadata:
  name: {{ include "dotnet-3-1-import-def-github.fullname" . }}
  labels:
    {{- include "dotnet-3-1-import-def-github.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dotnet-3-1-import-def-github.selectorLabels" . | nindent 4 }}
