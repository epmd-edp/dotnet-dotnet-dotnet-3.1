apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dotnet-3-1-import-def-github.fullname" . }}-test-connection"
  labels:
    {{- include "dotnet-3-1-import-def-github.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dotnet-3-1-import-def-github.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
